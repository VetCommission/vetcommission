"use client";
import { zodResolver } from "@hookform/resolvers/zod"; import { useMutation,useQueryClient } from "@tanstack/react-query"; import { Alert,TextField } from "@mui/material"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { useForm } from "react-hook-form"; import { z } from "zod"; import { useTenant } from "@/features/auth/TenantProvider"; import { CrudFormShell,type CrudFormMode } from "@/components/forms/CrudFormShell"; import { saveClinic,type Clinic } from "./clinicsApi";
const schema=z.object({name:z.string().trim().min(1,"Informe o nome.").max(160),legalName:z.string().max(200).optional(),document:z.string().max(30).optional(),email:z.string().email("Informe um e-mail valido.").optional().or(z.literal("")),phone:z.string().max(40).optional()}); type FormData=z.infer<typeof schema>;
export function ClinicForm({clinic,initialMode="view"}:{clinic?:Clinic;initialMode?:CrudFormMode}){const router=useRouter();const qc=useQueryClient();const{activeTenantId}=useTenant();const[mode,setMode]=useState<CrudFormMode>(clinic?initialMode:"create");const[error,setError]=useState<string|null>(null);const form=useForm<FormData>({resolver:zodResolver(schema),defaultValues:{name:clinic?.name??"",legalName:clinic?.legalName??"",document:clinic?.document??"",email:clinic?.email??"",phone:clinic?.phone??""}});const mutation=useMutation({mutationFn:(v:FormData)=>saveClinic(v,clinic?.id),onSuccess:async()=>{await qc.invalidateQueries({queryKey:["tenant",activeTenantId,"clinics"]});router.replace("/app/clinicas")},onError:()=>setError("Nao foi possivel salvar a clinica.")});return <CrudFormShell title={mode==="create"?"Nova clinica":"Clinica"} mode={mode} listHref="/app/clinicas" isSubmitting={form.formState.isSubmitting||mutation.isPending} onEdit={()=>setMode("edit")} onSubmit={form.handleSubmit(v=>{setError(null);mutation.mutate(v)})}>{error?<Alert severity="error">{error}</Alert>:null}<TextField fullWidth disabled={mode==="view"} label="Nome" error={Boolean(form.formState.errors.name)} helperText={form.formState.errors.name?.message} {...form.register("name")}/><TextField fullWidth disabled={mode==="view"} label="Nome juridico" {...form.register("legalName")}/><TextField fullWidth disabled={mode==="view"} label="Documento" {...form.register("document")}/><TextField fullWidth disabled={mode==="view"} label="E-mail" {...form.register("email")}/><TextField fullWidth disabled={mode==="view"} label="Telefone" {...form.register("phone")}/></CrudFormShell>}
