"use client";
import LogoutRoundedIcon from "@mui/icons-material/LogoutRounded";
import { AppBar, Box, Button, Container, FormControl, InputLabel, MenuItem, Paper, Select, Stack, Toolbar, Typography } from "@mui/material";
import Image from "next/image"; import Link from "next/link"; import { useRouter } from "next/navigation";
import { useAuth } from "./AuthProvider"; import { useTenant } from "./TenantProvider"; import { accessResources } from "./accessResources"; import { tenantHasAccess } from "./permissions";
export function AuthenticatedHome({area}:{area:"administrativa"|"do profissional"}){const router=useRouter();const{logout,session}=useAuth();const{activeTenantId,activeTenant,selectTenant,tenants}=useTenant();const handleLogout=()=>{logout();router.replace("/login")};return <Box sx={{minHeight:"100vh",bgcolor:"background.default"}}><AppBar color="inherit" elevation={0} position="static"><Toolbar sx={{gap:2}}><Image alt="VetCom" height={34} src="/assets/brand/vetcom-mark.svg" width={34}/><Box sx={{flexGrow:1}}><Typography sx={{fontWeight:700}}>VetCom</Typography><Typography color="text.secondary" variant="caption">Área {area}</Typography></Box><Button color="inherit" onClick={handleLogout} startIcon={<LogoutRoundedIcon/>}>Sair</Button></Toolbar></AppBar><Container component="main" maxWidth="xl" sx={{py:{xs:3,md:5}}}><Stack spacing={4}><Paper variant="outlined" sx={{p:{xs:3,md:5},background:"linear-gradient(135deg,rgba(25,118,210,.08),rgba(255,255,255,.9))"}}><Stack direction={{xs:"column",md:"row"}} spacing={3} sx={{alignItems:{md:"center"}}}><Box sx={{flexGrow:1}}><Typography component="h1" variant="h3">Olá, {session?.usuario.nome}</Typography><Typography color="text.secondary" sx={{mt:1}}>Você está acessando o tenant <Typography component="span" color="text.primary" sx={{fontWeight:700}}>{activeTenant?.nome}</Typography>.</Typography></Box><FormControl sx={{minWidth:{xs:"100%",md:280}}}><InputLabel id="tenant-select-label">Tenant ativo</InputLabel><Select disabled={tenants.length<=1} label="Tenant ativo" labelId="tenant-select-label" value={activeTenantId??""} onChange={event=>selectTenant(event.target.value)}>{tenants.map(tenant=><MenuItem key={tenant.tenantId} value={tenant.tenantId}>{tenant.nome}</MenuItem>)}</Select></FormControl></Stack></Paper><Box sx={{display:"grid",gridTemplateColumns:{xs:"1fr",sm:"repeat(2,1fr)",md:"repeat(3,1fr)"},gap:3}}>{tenantHasAccess(activeTenant,accessResources.professionalRolesManage)?<Paper variant="outlined" sx={{p:3}}><Stack spacing={2}><Typography variant="h5">Funções e cargos</Typography><Typography color="text.secondary">Mantenha os cargos padronizados.</Typography><Button component={Link} href="/app/funcoes-cargos" variant="contained">Acessar</Button></Stack></Paper>:null}{tenantHasAccess(activeTenant,accessResources.professionalSpecialtiesManage)?<Paper variant="outlined" sx={{p:3}}><Stack spacing={2}><Typography variant="h5">Especialidades</Typography><Typography color="text.secondary">Mantenha as especialidades padronizadas.</Typography><Button component={Link} href="/app/especialidades" variant="contained">Acessar</Button></Stack></Paper>:null}{tenantHasAccess(activeTenant,accessResources.professionalsManage)?<Paper variant="outlined" sx={{p:3}}><Stack spacing={2}><Typography variant="h5">Profissionais</Typography><Typography color="text.secondary">Cadastre, consulte e gerencie profissionais.</Typography><Button component={Link} href="/app/profissionais" variant="contained">Acessar</Button></Stack></Paper>:null}</Box></Stack></Container></Box>}
